# Terragrunt configuration for Dev environment
# Generated by Toyota Data Engineering Portal

# Configure Terragrunt to automatically store tfstate files in an S3 bucket
terraform {
  source = "."
}

# Include root terragrunt.hcl if it exists
# include "root" {
#   path = find_in_parent_folders()
# }

# Configure remote state (optional - uncomment if using S3 backend)
# remote_state {
#   backend = "s3"
#   config = {
#     bucket         = "your-terraform-state-bucket"
#     key            = "databricks/dev/terraform.tfstate"
#     region         = "us-east-2"
#     encrypt        = true
#     dynamodb_table = "terraform-locks"
#   }
# }

# Generate provider configuration
generate "provider" {
  path      = "provider.tf"
  if_exists = "overwrite_terragrunt"
  contents  = <<EOF
provider "databricks" {
  host = "https://dbc-32863125-bd1b.cloud.databricks.com"
  # Authentication via DATABRICKS_TOKEN environment variable
}
EOF
}
